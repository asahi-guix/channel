;; -*- mode: scheme; -*-

;; This is a base operating system configuration template for Guix
;; systems using the Asahi Linux kernel.

(use-modules (asahi guix system base))

(make-operating-system #:efi-uuid "9FBE-130E")
